# Decision log

Non-obvious choices, one line each, so nobody re-derives them next week:

```
[YYYY-MM-DD] <repo or "monorepo">: <what was decided> — <why, and what was rejected>.
```

**Read this before re-opening a settled question.** If a line here answers the choice in
front of you, follow it or change it deliberately; don't quietly decide the other way.

**Append when you pick A over B**, decline a dependency, or stop because a rule in a
`CLAUDE.md` forbade something. Not for what the code already says: a decision belongs here
when the code shows *what* was chosen and nothing shows *why*, or when the option that was
rejected left no trace.

Rules that keep this file readable:

- **One line.** If it needs a paragraph it needs an issue or a PR body, and the line links
  to that. A decision log that grows paragraphs stops being read, and an unread log is
  worse than none: it looks like the question was answered.
- **Name the rejected option.** "Chose X" is a note; "chose X over Y because Z" is a
  decision.
- **Append at the end**, newest last. Two agents appending at once conflict on the last
  line; the resolution is always to keep both lines.
- **Per repo, not here.** A decision about one service goes in that repo's own
  `DECISIONS.md` (create it with your first line), in the same PR as the change it
  explains. This file is for choices that span repos or live in the infrastructure.

The log starts here, so a missing entry is not evidence that nothing was decided: older
choices live in git history and in the `CLAUDE.md` rules they turned into.

---

[2026-08-31] fantasy-bff: a premium game range is refused by the BFF, not merely hidden in the web (#187) — a hidden control is not a gate, and the entitlement has to hold for any caller of the API.
[2026-09-09] monorepo: adopted this log, plus "Fix from first principles" and "Definition of done" in the root `CLAUDE.md` — taken from the Accounted repo, whose own log had drifted into paragraphs, hence the one-line rule above.
[2026-09-09] monorepo: dropped the em dash rule for app copy, from CLAUDE.md and the slapstat-copy skill (#36) — it cost more in correction than it bought, and its "at most one per screen" half was never checkable from one file; keeping prose from turning breathless is the slapstat-copy voice section's job now.
[2026-09-10] monorepo: the Sentry round runs locally as a Claude desktop scheduled task on Alexander's subscription, not as a GitHub Action on an Anthropic API key — he declined per-token spend; because it runs with his own credentials it starts in its own guarded folder (C:/Users/Alexander/sentry-triage) whose PreToolUse guard blocks servers, databases, credentials, network egress and its own settings, since deny rules only match the usual spelling of a command and the Claude Code sandbox does not run on native Windows.
[2026-09-10] monorepo: em dashes are banned in app copy again, enforced by fantasy-web's `npm run check:copy` (fantasy-web#593) — reverses the 2026-09-09 entry above: a flat ban a script checks on every PR has neither of that rule's objections.
[2026-09-11] bff + web: a pool reconciliation reports the ids of the rows it added (`poolReconciliation.addedPlayerIds`), replacing the count (fantasy-bff#216, fantasy-web#618) — the web narrows the table to those rows from the notice, which a count cannot do; the count was dropped rather than kept beside the list (two fields that must agree is one more than needed), and a stored per-row marker was rejected because nothing defines when it would be cleared.
[2026-09-11] db + bff + web: a reconciliation's added players are stored on the projection (`settings.unacknowledgedNewPlayerIds`) until the owner presses "Got it" (fantasy-db-service#104, fantasy-bff#218, fantasy-web#625) — Alexander wanted the notice to survive a reload; localStorage per projection was rejected (only the first device would see it), and the stored marker the entry above rejected for want of a defined clear is what the acknowledgement now is.
[2026-09-11] monorepo: `projection-sync.sh` finds its container by the Coolify label `coolify.serviceName` ending in `-projection-service`, replacing staging's hard-coded application UUID, so one file serves staging and production (#50) — reading the UUID from the host's `/root/health-monitor.conf` was rejected for moving the per-host constant into a second hand-copied file; a renamed Coolify app fails the lookup loudly, and `--find-container` checks it at install.
[2026-09-11] monorepo: a failed promotion opens a `prod-promotion-failed` issue in its own repo, from six identical copies of `promote-to-prod.yml` rather than one reusable workflow here (fantasy-workspace#30) — sharing needs an Actions settings change in all seven repos and puts every prod deploy behind this repo's master; one issue here was rejected because the default token cannot write across repos and a PAT is one more secret.
[2026-09-11] monorepo: `projection-sync.sh` runs `projection game-logs` only in season (the morning after opening night to June) and `projection schedule` every night (#53, fantasy-projection-service#164, #165) — logs feed Who's hot, the schedule is what a return date is read off; the October month rule was rejected (2026-27 opens in September) and year-round logs as a re-fetch of a finished season; the schedule runs year-round at Alexander's call, so a summer projection counts return dates on the coming season's calendar.
[2026-09-11] monorepo: a failed nightly sync is retried once by `projection-sync-retry.timer` at 16:30 UTC reading a state file, and reaches Sentry only if the retry fails too (#53) — most failures fix themselves within hours, and an alert per transient trains everyone to ignore alerts; a retry loop inside the run was rejected (it holds the unit open past its 90-minute cap), and an `OnFailure=` transient timer because it is invisible in the repo and does not survive a reboot.
[2026-09-11] monorepo: `BLUEPRINT.md` is folded into `INFRASTRUCTURE.md` (one level above the checkout, outside git since #24), now the single blueprint — the two covered the same decisions and had drifted apart (`BLUEPRINT.md` still advised a parent folder that is not a repo, `git add -A` and promotion through the Coolify UI); keeping both was rejected as two copies of one rule set.
[2026-09-11] monorepo: `set_staging_version_forced.sh` warns on a webhook deployment from the last 30 minutes and still deploys (#58) — refusing like `promote_forced.sh` was rejected: on staging the stamped deploy is the cure for the race, so refusing would leave the unstamped webhook build as the only one.
[2026-09-11] monorepo: `set_staging_version_forced.sh` stamps `SENTRY_RELEASE` only where the staging app already has the variable, warning otherwise, and never for fantasy-web (#61) — creating it from the script was rejected (it widens six CI keys from updating values to adding variables), and failing was rejected because no staging app has the variable yet, so every repo's merge would go red at once.
[2026-09-13] monorepo: the four Spring services run the unpacked boot jar (`java -Djarmode=tools -jar app.jar extract`; bff#233, db#115, yahoo#54, espn#28) — staging's BFF deadlocked on 2026-09-11 with both carriers pinned in class loading behind a lock in `NestedJarFile`; raising scheduler parallelism was rejected as making the deadlock rarer, and turning virtual threads off as against the services' concurrency model.
[2026-09-13] monorepo: a health-monitor outage emails Alexander through Sentry alert rule 821976 (`monitor=downtime`, 5-minute throttle) — the 2026-09-11 bff outage grouped onto an older issue and the project's only rule let it through silently; a rule on new issues was rejected for failing the same way, and mail from `health-monitor.sh` itself for adding a secret to hand-copy onto both boxes. The rule lives outside git, created through Sentry's API.
[2026-09-15] yahoo + bff + web: Yahoo refusing a request reaches the web as 424 `YAHOO_ACCESS_DENIED` with Yahoo's own sentence, relayed by yahoo-service as 403 — until 2026-09-15 it travelled as a 502 the web retried while the admin page said "Connected"; 403 to the web was rejected (the web reads it as a Premium refusal, and the auth interceptor as a dead refresh token), and 502 with a code because the web retries every 502.
[2026-09-15] monorepo: with Yahoo's access restored the Yahoo player sync runs nightly again while `PLAYERS_SOURCE` stays `espn` — the read model's one reader is the admin id remap, and staging still holds projections and shares on Yahoo ids, so the crosswalk stays fresh until they are remapped; switching the pool back was rejected (saved projections are on ESPN ids), and keeping the sync off while the remap needs it; switching the schedule off once staging's leftovers are remapped is Alexander's call.
[2026-09-15] db + bff + web: the player pool moves back from ESPN to Yahoo (`PLAYERS_SOURCE=yahoo`) at Alexander's call, reversing the entry above, but only behind a migration — the id remap runs in either direction (fantasy-db-service#117), a share is stamped with its projection's id space, and a projection keyed by the other platform is neither squared with the pool nor saved over; flipping the flag alone would add the whole Yahoo pool beside every ESPN-keyed projection and the next save would drop what the pool cannot draw. Order per environment: deploy, dry run, apply, set the flag, redeploy the BFF, confirm the dry run scans 0; production only on Alexander's go-ahead.
[2026-09-15] monorepo: the six `claude.yml` workflows are deleted rather than gated to `github.actor == 'pgaberra'` — the repos went public, so anyone can write the text a run reads, and the workflow never ran; a gate was rejected because it stops strangers starting a run, not their text reaching it, and it keeps a write-scoped job one secret from live. Restoring it is a revert plus a secret.
[2026-09-15] monorepo: the Sentry round counts only what `pgaberra` wrote on GitHub (watermark markers and matching dedupe), ignores a watermark dated after its own start, finds its run log by number and writes nothing about who hit a fault — the repos went public, so any GitHub user could post a far-future watermark and blind the round, and it had already copied a user's city and device into public threads; locking alone was rejected because dedupe searches every issue in seven repos, which cannot all be locked.
[2026-09-15] monorepo: after the repos went public, every repo got a `protect-master` ruleset (required check and up-to-date branch in the six with CI; deletion, force-push and PR-only in fantasy-workspace), secret scanning with push protection, private vulnerability reporting, Dependabot alerts, approval for outside fork-PR runs, and no wiki — a required reviewer on `production` was not added (only Alexander can publish, and it adds a click per release without keeping anyone out), and `PROD_PROMOTE_SSH_KEY` stays a repo secret because GitHub never hands a secret's value back to move.
[2026-09-15] monorepo: Dependabot PRs in the six service repos are merged by the scheduled Claude round (`.claude/skills/dependabot-round/SKILL.md`, run log #70), held to Dependabot's own PRs by an allowlist guard checking author, branch, commits and pinned head — GitHub auto-merge was rejected: the rulesets require up-to-date branches, a branch update pushed with `GITHUB_TOKEN` starts no CI, so it would need a long-lived PAT in six public repos; the round files an issue for a red PR rather than fixing it.
[2026-09-15] monorepo: the Sentry round writes every issue, comment and PR body to a file and posts it with `--body-file`, rather than the guard learning to skip `--body` text — the guard once refused a correct diagnosis for suggesting a look at the deploy history, and the watermark stuck; teaching the guard to parse `gh` arguments was rejected as widening the one boundary between attacker-influenced text and Alexander's credentials, and letting `gh release` read too, so the round names a possible deploy as unchecked instead of looking.
[2026-09-16] monorepo: fantasy-projection-service is private again, a day and a half after the seven repos went public, while the other six stay public — it holds the paid AI projection (model, tuned constants, backtests), the one thing SlapStat charges for; keeping it public was rejected (the openness buys nothing the other six don't already show), and moving the model's constants out as a larger change for the same protection. The cost: its `protect-master` ruleset and secret scanning stop applying (GitHub Free), Actions minutes count again, and bff's `SPEC_READ_TOKEN` cannot be dropped.
[2026-09-16] monorepo: the backend services are reachable from the internet only where a browser must reach them — yahoo-service's public host serves just `/api/v1/yahoo/oauth/callback` in both environments (explicit Traefik labels, strip prefix off; see fantasy-yahoo-service `DEPLOYMENT.md`), staging espn and projection lost their `sslip.io` domains, and the BFF reaches everything on the Docker network; checking the key more strictly in code was rejected (a leaked key would still open everything), and the path in Coolify's domain field because Coolify regenerates the labels with a strip-prefix middleware that broke the callback and dropped the rate limit.
[2026-09-16] monorepo: the six repos keep their own copy of `promote-to-prod.yml` rather than one reusable workflow here (copies still byte-identical today) — the three old reasons partly fell, but a change still takes six PRs (each caller must bump its SHA), the prod SSH key would be handed to code in the one repo whose master no check guards, and a SHA bump reviewed as a hash is easier to wave through than the diff itself, while the migration could only be tested by publishing six releases.
[2026-09-16] monorepo: a Yahoo connect is bound to the SlapStat account that started it by a claim step — yahoo-service parks the tokens under a one-time code in the web URL fragment, the signed-in web claims them through the BFF, and a claim by any other account discards them (yahoo-service#63, bff#253, web#684) — a cookie on yahoo.slapstat.com set by a start redirect was rejected (it adds a second public route and still would not prove the SlapStat user); prod releases must go bff, web, then yahoo-service.
[2026-09-16] monorepo: the Coolify panel (`coolify.slapstat.com`, API and websockets) answers only Alexander's home address, the staging server and the prod server; everything else gets 403 from Traefik via `/data/coolify/proxy/dynamic/coolify-ip-gate.yaml` on the prod host, copying Coolify's routers at a higher priority because Coolify regenerates its own file on updates — Cloudflare Access was passed over (the panel is not used away from home), and restricting SSH to known addresses was rejected (GitHub runners deploy over SSH from changing Azure addresses; SSH is already key-only with fail2ban and forced-command CI keys). If the home address changes, the file's `sourceRange` must follow.
[2026-09-17] monorepo: staging is locked to the owner's IP by default, and the E2E suite gets through by asking the staging-access forced command to `allow` its own runner's IP (taken from the SSH connection, expiring after 90 minutes) and `revoke` it afterwards — rejected: allowlisting GitHub's published runner ranges (open in all but name), running the suite from Alexander's machine (nothing runs it when he is away), and leaving staging open as before; Sentry's staging uptime check was disabled rather than allowlisting its checker IPs, since it only measured the gate.
[2026-09-17] monorepo: Stripe's published webhook IPs are let through staging's locked HTTPS gate by a static list (`ip-allowlist.staging.extra`, installed as `/root/ip-allowlist.extra`) that `ip-allowlist.sh` applies on boot and on every lock — fetching Stripe's list at apply time was rejected (a boot that cannot reach stripe.com would leave the gate half applied), and opening staging while testing payments (a forgotten window stays open); `ip-allowlist.sh` is versioned in the monorepo from this change on.
[2026-09-17] monorepo: staging's user data is reset nightly (05:00 UTC, before E2E) by a workflow here: every account but one admin (secret-named, kept so its Yahoo link survives) is deleted and three seeded accounts are written straight into db-service's tables with bcrypt hashes from secrets, over a forced-command key that backs up and runs piped SQL on the three staging databases (fantasy-db-service#45, `staging-reset-setup.md`) — rejected: a keep-list, recreating the admin, a pg_dump restore (drifts at the next migration), seeding through sign-up (sends mail, fails with sign-up), a host systemd timer (fails silently), and the reset logic in the forced command (every change would need a reinstall on the box); the price is that the key can run any SQL on those three staging databases.
[2026-09-17] monorepo: staging's HTTPS gate (`ip-allowlist.sh`) drops UDP 443 as well as TCP 443 — Traefik serves HTTP/3 there and Docker publishes the port, so a TCP-only gate stood open to any HTTP/3 client, closed only by Hetzner's Cloud Firewall happening to allow TCP alone; turning HTTP/3 off in staging's Traefik was rejected as leaving the gate as wrong as before for whoever turns it back on.
[2026-09-17] monorepo: `ip-allowlist.sh` tags every rule it writes (`ip-allowlist`) and removes its old rules by that tag, plus any untagged 443 RETURN/DROP an earlier version left, instead of deleting the rule it would write this run — delete-by-value was rejected because it never removed a former owner IP's RETURN when `ALLOW_IP` changed, so that address stayed let in; a changed owner IP is made lasting by changing the script's default, since a command-line `ALLOW_IP` is overwritten by the next boot or lock.
[2026-09-17] monorepo: production serves the Yahoo player pool (`PLAYERS_SOURCE=yahoo` since 15:27 UTC, bff v0.96.0, db v0.43.0), completing the 2026-09-15 entry — the remap matched 98.9% of ESPN's players with games, and Alexander accepted losing the eleven it did not (Toews, Kopitar, Quick and others, their rows left on ESPN ids and unseen) rather than hand-matching or blocking the switch on them; the pre-apply backup of `user_projections` and `projection_shares` stays on the prod host (`/root/backups/`), not copied off it, since it holds user data.
[2026-09-17] monorepo: the Dependabot round runs once a week (Monday 09:00) instead of every three hours, at Alexander's call, and every service's Dependabot gets a seven-day cooldown and a Sunday 22:00 Europe/Stockholm run — unattended merging removed the human moment in which a freshly compromised release gets noticed, and a week matches the round; checking release age inside the round was rejected (its guard allows no network reads beyond `gh`), and security updates still bypass the cooldown, at the price of waiting up to a week for the round.
[2026-09-17] monorepo: every repo's release series moved from 0.x to 1.0.0 at Alexander's call (in production with paying users), by one `feat!:` PR per repo squash-merged with a `BREAKING CHANGE:` footer (tag-on-merge.yml's action parses commits with the default Angular preset and ignores a bare `!`), and the unused build-file versions went with it — pushing a hand-made `v1.0.0` tag was rejected, it skips the draft release and the staging stamp that only the merge path creates; from here a `BREAKING CHANGE:` footer means a real major version, never an internal API change.
[2026-09-18] db + bff + web: a share link follows its projection (fantasy-db-service#135, fantasy-bff#277, fantasy-web#766), reversing fantasy-web#295's one-way snapshot at Alexander's call ("always live") — edits made after sharing never reached the link, and a board edited in the same session was published with the stats it was opened with. Rejected: a truly live per-read page (the BFF would need a second copy of the scoring formula in Java), a new token per re-share (leaves old public pages behind), and an Update button (Alexander declined in favour of Share just showing the link).
[2026-09-18] db + bff + web: copying a shared board sends the `updatedAt` the page read, and a board its author changed since is refused with 412 and read again, with a note saying nothing was copied (fantasy-db-service#137, fantasy-bff#279, fantasy-web#769) — Alexander's call, since a link now follows its projection and the copy used to be of the latest board, which the reader never saw. Rejected: keeping every published version (a version store for boards the author has replaced), copying the latest and telling the reader afterwards (in Draft Mode they learn mid-draft), and polling (every open link a request every few seconds). 412 over 409 so a stale read cannot be taken for a taken name.
[2026-09-19] monorepo: SlapStat's injury handling is ESPN's feed alone, so the `injury-round` skill, its run log (#93) and guarded folder are removed, and `projection-sync.sh` goes back to injuries before lines (reverting #94 and #95, and fantasy-projection-service#202 and #205) — Alexander's call hours after approving the design: the round existed only to keep a hand-kept return-date register filled, no permitted source carries dates for the players ESPN misses, and a standing chore for four players a year is not worth a third scheduled round; keeping the round without the register was rejected (it would have nothing to write) and lines-before-injuries on its own merits (with the register gone neither step reads the other).
[2026-09-21] monorepo: the root `CLAUDE.md` is slimmed by a third (18.2k → 12.4k chars) and the incident stories behind its rules moved to the new `AGENT-NOTES.md`, read on demand — `CLAUDE.md` rides in every AI request in every repo, so its narrative was a per-request token cost Alexander was paying on every turn of every session; deleting the stories outright was rejected because they are what stops the rules being quietly bent, going telegraphic past the rule floor and slimming the per-repo `CLAUDE.md` files the same way were left for Alexander to call.
[2026-09-21] db + bff + web: a shared link taken into an account is now two separate things — a *follow* (db#139: an `IMPORTED` row mirrored from the share on every author publish, read-only but for its own draft, one per user and share, deleted by FK cascade when the share goes) and a *copy* (`POST /projections/copies`, an ordinary board named "Copy of X" with no link back, db#143 removing the follow it used to create alongside), with the web grouping on ownership (`origin != null`) rather than `kind`, so a spreadsheet upload sits with the user's own boards and "Imports" became "Following" (web#792, #797, bff#285, #288) — keeping the frozen editable import was rejected (the author's later work never reached the reader, and a second import just made "X (2)"), and letting one press do both was rejected once it meant a press on "Create a copy" silently produced two rows.
